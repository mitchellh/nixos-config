.DS_Store
backup.tar.gz
result
iso/nixos.iso

# Local scratch copy of the generated dataset. The canonical dataset now lives
# outside the repo and is passed in via `--override-input generated ...`.
generated/

# bv (beads viewer) local config and caches
.bv/

# beads daemon socket (can't be copied by Nix over shared folders)
.beads/*.sock

.opencode/
.logs/
.claude/
.github/*
!.github/workflows/
!.github/workflows/**
results/
.vscode
.webui_secret_key
plugins/niri-deep/target
# .agents/.gitignore is generated by dotagents — don't commit it.
.agents/.gitignore


# APM dependencies
apm_modules/

# APM-generated Claude commands
.claude/commands/*-apm.md


# APM integrated prompts
.github/prompts/*-apm.prompt.md


# APM integrated agents
.github/agents/*-apm.agent.md
.github/agents/*-apm.chatmode.md
.worktrees/
